{
  "name": "conde-nast-traveller-concepts",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build && npm run build:paris-github-pages",
    "build:paris-github-pages": "npm ci --prefix paris-hotels-mobile && npm run build:github-pages --prefix paris-hotels-mobile",
    "preview": "vite preview"
  },
  "devDependencies": {
    "vite": "^6.0.0"
  }
}
